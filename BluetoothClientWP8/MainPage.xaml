<phone:PhoneApplicationPage Loaded="PhoneApplicationPage_Loaded"
    x:Class="BluetoothClientWP8.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <!--<Grid x:Name="LayoutRoot" Background="Transparent">-->
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
        </Grid.RowDefinitions>
        <Button Name="ConnectAppToDeviceButton" Click="ConnectAppToDeviceButton_Click_1" Width="200" Height="100" Content="Connect" Margin="10,10,270,658"/>
        
        <Button Name="RedButton" Click="RedButton_Click_1" Width="200" Height="100" Content="Red" Margin="136,171,144,497"/>
        <Button Name="GreenButton" Click="GreenButton_Click_1" Width="200" Height="100" Content="Green" Margin="136,276,144,392"/>
        <Button Name="YellowButton" Click="YellowButton_Click_1" Width="200" Height="100" Content="Yellow" Margin="136,381,144,287"/>
        <TextBox x:Name="DeviceName" HorizontalAlignment="Left" Height="72" Margin="215,25,0,0" TextWrapping="Wrap" Text="linvor" VerticalAlignment="Top" Width="255"/>
        <TextBlock x:Name="BodyDetectionStatus" HorizontalAlignment="Left" Margin="10,408,0,0" TextWrapping="Wrap" Text="No body detected" VerticalAlignment="Top" Width="460" FontSize="36" TextAlignment="Center"/>-->

        <!--<StackPanel Grid.Row="1">
            <Button x:Name="OneShotLocationButton" Click="OneShotLocation_Click" Content="get one-shot location"/>
            <Button x:Name="TrackLocationButton" Click="TrackLocation_Click" Content="track location"/>
            <StackPanel Orientation="Horizontal">
                <TextBlock>latitude:</TextBlock>
                <TextBlock x:Name="LatitudeTextBlock"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock>longitude:</TextBlock>
                <TextBlock x:Name="LongitudeTextBlock"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock>status:</TextBlock>
                <TextBlock x:Name="StatusTextBlock"/>
            </StackPanel>
        </StackPanel>
    </Grid>-->


    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--TitlePanel contains the name of the application and page title--><!--
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="MY APPLICATION" Style="{StaticResource PhoneTextNormalStyle}" Margin="12,0"/>
            <TextBlock Text="page name" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>-->

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Margin="12,0,12,0">
            <StackPanel Grid.Row="0">
                <Button x:Name="OneShotLocationButton" Click="OneShotLocation_Click" Content="get one-shot location"/>
                <Button x:Name="TrackLocationButton" Click="TrackLocation_Click" Content="track location"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock>latitude:</TextBlock>
                    <TextBlock x:Name="LatitudeTextBlock"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock>longitude:</TextBlock>
                    <TextBlock x:Name="LongitudeTextBlock"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock>status:</TextBlock>
                    <TextBlock x:Name="StatusTextBlock"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="1">
            <Button  x:Name="ConnectAppToDeviceButton" Click="ConnectAppToDeviceButton_Click_1" Width="200" Height="100" Content="Connect" Margin="0,10,270,68"/>
            <Button  x:Name="btnSend_CoOrd" Click="btnSend_CoOrd_Click" Width="200" Height="100" Content="Send Co-Ord" Margin="10,10,270,758"/>
        </StackPanel>

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
    </Grid>
    
    
</phone:PhoneApplicationPage>